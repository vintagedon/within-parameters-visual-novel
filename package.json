{
  "name": "within-parameters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "replay": "node scripts/run-replay.mjs 5000",
    "replay:fast": "node scripts/run-replay.mjs 2000",
    "replay:full": "node scripts/run-replay.mjs 10000",
    "test:screens": "/opt/agents/venv/bin/python tests/capture.py",
    "test:screens:check": "/opt/agents/venv/bin/python tests/capture.py --check"
  },
  "devDependencies": {
    "@types/node": "^25.5.0",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
